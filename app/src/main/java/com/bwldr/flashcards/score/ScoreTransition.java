package com.bwldr.flashcards.score;

/**
 * Represents the 3 types of transitions that can happen
 * when going from one Card to the next
 */
public enum ScoreTransition {
    NEXT,
    RETRY,
    FINISH
}
